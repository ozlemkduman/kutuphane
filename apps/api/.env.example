# ============================================
# Kütüphane API - Environment Configuration
# ============================================
# Bu dosyayı .env olarak kopyalayın ve değerleri doldurun

# ============================================
# DATABASE (Zorunlu)
# ============================================
# PostgreSQL bağlantı URL'i
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# Örnek (localhost):
# DATABASE_URL="postgresql://kutuphane:kutuphane123@localhost:5432/kutuphane_db"

# ============================================
# FIREBASE (Zorunlu)
# ============================================
# Firebase Console > Project Settings > General
FIREBASE_API_KEY="your-api-key"
FIREBASE_AUTH_DOMAIN="your-project.firebaseapp.com"
FIREBASE_PROJECT_ID="your-project-id"

# Firebase Admin SDK için (service account)
# Firebase Console > Project Settings > Service Accounts > Generate new private key
# İndirilen JSON dosyasını apps/api/firebase-service-account.json olarak kaydedin
# veya aşağıdaki değerleri doldurun:
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL="firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com"

# ============================================
# SERVER (Opsiyonel)
# ============================================
# API port (varsayılan: 3001)
PORT=3001

# CORS - İzin verilen origin'ler (virgülle ayırın)
CORS_ORIGINS="http://localhost:3000,https://yourdomain.com"

# Node environment
NODE_ENV=development

# ============================================
# ENCRYPTION (Opsiyonel - SMTP şifreleme için)
# ============================================
# 32 karakter (256-bit) rastgele hex string
# Oluşturmak için: openssl rand -hex 32
ENCRYPTION_KEY="your-32-char-hex-encryption-key-here"

# ============================================
# RATE LIMITING (Opsiyonel)
# ============================================
# Varsayılan değerler app.module.ts'de tanımlı
# THROTTLE_SHORT_TTL=1000
# THROTTLE_SHORT_LIMIT=30
# THROTTLE_MEDIUM_TTL=10000
# THROTTLE_MEDIUM_LIMIT=100
# THROTTLE_LONG_TTL=60000
# THROTTLE_LONG_LIMIT=500
